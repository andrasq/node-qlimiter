{
  "name": "qlimiter",
  "description": "fast nodejs call rate limiter",
  "version": "0.2.0-dev",
  "license": "Apache-2.0",
  "main": "qlimiter.js",
  "repository": "git://github.com/andrasq/node-qlimiter",
  "author": "Andras (http://github.com/andrasq)",
  "engines": {
    "node": ">=0.10"
  },
  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc --version && nyc --reporter lcov --reporter text qnit test-* || echo nyc not installed"
  },
  "keywords": [
    "call", "rate", "limit", "throttle", "Andras", "limiter", "capped"
  ],
  "dependencies": {
    "qlist": "0.12.0"
  },
  "devDependencies": {
    "qnit": "0.11.0"
  }
}
